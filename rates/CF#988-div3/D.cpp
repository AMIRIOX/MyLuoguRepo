#include <bits/stdc++.h>
using namespace std;

#define int long long

bool check() {

}

void solve() {
    int n, m;
    vector<int> a(n);
    for(int i = 1; i <= n; i++) {
        
    }
}

signed main() {
    cin.tie(0) -> sync_with_stdio(false);
    int t = 1; cin >> t;
    while(t--) solve();
    return 0;
}
