#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int n;
    vector<ll> a(n);
    for(auto &i : a) cin >> i;
    for(ll i = -1e5; i <= 1e5; i++) {
        
    }
}

signed main() {
    cin.tie(0) -> sync_with_stdio(false);
    int tt = 1; cin >> tt;
    while(tt--) solve();
    return 0;
}
