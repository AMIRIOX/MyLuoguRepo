#include <iostream>
#include <cstdio>
using namespace std;
int n,x,p,q;
const int maxn=1e5+10;
int t[maxn];
int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	cin >> n >> x >> p >> q;
	// 
	return 0;
}